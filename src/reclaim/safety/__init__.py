"""L3 Safety Gate — the single deterministic choke point for every mutation.

Everything below the gate is deterministic and testable without an LLM; the AI sits strictly
above it and holds no destructive capability (ARCHITECTURE.md §1, §3).
"""
